import { RestInputDirective } from './rest-input.directive';

describe('RestInputDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new RestInputDirective();
    expect(directive).toBeTruthy();
  });
});
